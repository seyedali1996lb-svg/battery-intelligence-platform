# src/ — Battery Intelligence Platform core modules.
#
# This package contains the platform's backend logic: database, API,
# adapters, analytics, and utilities. It is importable as a package
# (from X import ...) when the project root is on sys.path.
