# The batlab package itself (app/main.py and most app/_pages/*.py now import
# from it) — Streamlit Community Cloud only installs from this file, not
# pyproject.toml, so this line is required for the deployed app to start.
-e .

pandas
pyarrow
openpyxl
scikit-learn
scipy
plotly
streamlit
requests
reportlab
anthropic
pybamm
paho-mqtt
fastapi[standard]
uvicorn
qrcode[pil]
sqlalchemy
# PostgreSQL driver — only used when DATABASE_URL points at postgres
# (SQLite is the default); see scripts/migrate_sqlite_to_postgres.py
psycopg2-binary
pytest
bcrypt
mat-io
PyJWT
cryptography
networkx
